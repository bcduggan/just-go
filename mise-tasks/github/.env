#!/usr/bin/sh

set -euf

JQ_QUERY='
  { "REPO_ID": .id, "REPO_NAME": .name, "REPO_OWNER": .owner.login }
  | to_entries[]
  | [.key, (.value|@sh)]
  | join("=")
'

set -a

. /dev/stdin <<EOF
$(gh repo view --json id,owner,name --jq "$JQ_QUERY")
EOF
